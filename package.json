{
  "name": "codesw",
  "version": "0.1.0",
  "description": "Switch model providers for Claude Code / OpenCode / OpenClaw / Hermes Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "codesw": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepublishOnly": "yarn build"
  },
  "keywords": ["claude-code", "opencode", "openclaw", "hermes-agent", "cli"],
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "json5": "^2.2.3"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.12.0",
    "typescript": "^5.4.0"
  }
}
